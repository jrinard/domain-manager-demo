import { Meta } from '@storybook/blocks';

<Meta title="DL: Falcon/Molecules/Select/Explanation" />

# Description
Currently, where one would use `Select` Falcon is `Combobox`. This does mean that mobile does not get the benefits of a native select nor is it WAI-ARIA compliant ("accessible").

