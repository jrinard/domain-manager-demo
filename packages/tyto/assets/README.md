# @tyto/assets

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @tyto/assets` to build the library.

## Running unit tests

Run `nx test @tyto/assets` to execute the unit tests via [Jest](https://jestjs.io).
