# @tyto/msw

Provides a [fake](https://martinfowler.com/bliki/TestDouble.html) Tyto server for HTTP

`@tyto/msw` provides the behavior whereas `@tyto/lore` defines the fixed responses of the endpoints.
