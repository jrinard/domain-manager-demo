# origins

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test origins` to execute the unit tests via [Vitest](https://vitest.dev/).

## Purpose
This package is a simple, lightweight library for retrieving parent and top-most origins for a given window via `window.location.ancestorOrigins`.
