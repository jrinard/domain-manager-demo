# Demo assets

Static images for the portfolio demo. Served at `/demo-assets/...`.

Re-download from Cherry (one-time): `./scripts/download-demo-assets.sh`

Swap any file here to change what the demo shows — keep the filename or update paths in `src/demo/demoAssetPaths.ts` and `MenuDataFallback.ts`.
