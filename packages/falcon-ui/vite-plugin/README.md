# falcon-ui-vite-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build falcon-ui-vite-plugin` to build the library.

## Running unit tests

Run `nx test falcon-ui-vite-plugin` to execute the unit tests via [Jest](https://jestjs.io).
