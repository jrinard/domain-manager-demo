# @tyto/manifest

Provides reused `Types` for Tyto [Resources](https://www.w3schools.in/restful-web-services/rest-resources) but not the Endpoints. Please do not include the type for the top-level endpoint resource (e.g. `Event` for `GET /event -> response.event`). That should stay with the Resource in Tyto Client
