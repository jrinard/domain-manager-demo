# tricorder

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tricorder` to build the library.

## Running unit tests

Run `nx test tricorder` to execute the unit tests via [Jest](https://jestjs.io).
